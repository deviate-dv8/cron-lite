# Node
TZ=UTC
PORT=3333
HOST=0.0.0.0
NODE_ENV=development

# App
LOG_LEVEL=info
APP_KEY=docker-adonis-ak
APP_URL=http://${HOST}:${PORT}

# Session
SESSION_DRIVER=cookie

# PG Database
DB_HOST=database
DB_PORT=5432
DB_USER=user
DB_PASSWORD=pass
DB_DATABASE=cron-lite

#--------------------------------------------------------------------
# CORS (configure allowed origins for API access)
#--------------------------------------------------------------------
# CORS_ORIGIN=http://localhost:5173,http://localhost:3000
